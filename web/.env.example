# Host API (pnpm dev + dev:infra) runs on 3001, web on 3002 — no collision with docker 3000
NEXT_PUBLIC_API_URL=http://localhost:3001
# For docker-only mode (pnpm dev:docker) where API is container 3000: NEXT_PUBLIC_API_URL=http://localhost:3000
